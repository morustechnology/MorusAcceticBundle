{% block accetic_invoices_widget %}
    {% spaceless %}
        {% for invoice in form %}
            {{ form_widget(invoice) }}
        {% endfor %}
        {{ form_rest(form) }}
    {% endspaceless %}
{% endblock accetic_invoices_widget %}

{% block accetic_invoice_widget %}
    {% spaceless %}
        <tr id="{{ form.vars.id }}" class="test">
            <td>{{ form_widget(form.parts)}}</td>
            <td>{{ form_widget(form.description)}}</td>
            <td>{{ form_widget(form.qty) }}</td>
            <td>{{ form_widget(form.sellprice)}}</td>
            <td>{{ form_widget(form.discount)}}</td>
            <td><label id="{{ form.vars.id }}_amount"></label></td>
            <td></td>
        </tr>
    {% endspaceless %}
{% endblock accetic_invoice_widget %}